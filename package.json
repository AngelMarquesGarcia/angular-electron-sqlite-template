{
  "name": "from-scratch-angular-electron",
  "version": "0.0.0",
  "main": "dist/main/electron/main.js",
  "scripts": {
    "start": "npm run compile:electron && npm-run-all start:angular start:electron-serve",
    "start:local": "npm run compile && npm --prefix electron run start:local",
    "compile": "npm run compile:angular && npm run compile:electron",
    "compile:angular": "npm --prefix angular run build:dev",
    "compile:electron": "npm --prefix electron run build",
    "start:angular": "npm --prefix angular run start",
    "start:electron-serve": "wait-on tcp:4200 && npm --prefix electron run start:serve",
    "start:electron-local": "npm run compile:angular && npm --prefix electron run start:local",
    "predebug": "npm predebug:angular && npm predebug:electron",
    "predebug:angular": "npm --prefix angular run start:dev",
    "predebug:electron": "npm --prefix electron run build",
    "compile-and-make": "npm run compile && npm run forge-make"
  },
  "private": true,
  "dependencies": {
    "better-sqlite3": "^12.6.2"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.4",
    "electron": "^40.2.1",
    "electron-builder": "^26.7.0",
    "kill-port": "^2.0.1",
    "npm-run-all": "^4.1.5",
    "wait-on": "^9.0.3"
  }
}
